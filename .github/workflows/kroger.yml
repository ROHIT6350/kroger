name: new code kroger

on: [push]

jobs:
  build: 
    runs-on: ubuntu-latest

    steps:
      - name: wel-come 
        run: echo "hello rohit"
      - name: adding new file
        run: |
          touch file1
          echo " hello kroger team" > file1
          date
          cat file1
      
